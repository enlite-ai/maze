# .readthedocs.yml
# Read the Docs configuration file
# See https://docs.readthedocs.com/platform/stable/build-customization.html for details

version: 2

sphinx:
   configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
