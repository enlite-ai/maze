"""Docs must be defined as package in order do be tested.

(The package "docs" is already defined in the python environment, leaving this docs directory as namespace package
would therefore not work)
"""
